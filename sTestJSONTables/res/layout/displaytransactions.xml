<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" 
    android:orientation="vertical"
    android:layout_marginTop="5dp"
    android:id="@+id/transactionsLinearLayout">

	<TextView
	    android:id="@+id/chooseCatDisp"
	    android:layout_width="match_parent"
	    android:layout_height="wrap_content"
	    android:text="Please choose category for each store" />
    
    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:numColumns="1" 
        android:id="@+id/listViewTransactions"
        android:visibility="invisible">
    </ListView>
    <Button android:id="@+id/addStoreToDB"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Store categories to DB" />
	
</LinearLayout>
